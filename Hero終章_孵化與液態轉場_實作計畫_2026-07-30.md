# Hero-Lab 終章：生物孵化與液態轉場 — 實作計畫書（細節版 v2）

- **文件版本**：v2（2026-07-31，細化自 v1）
- **狀態**：待動工（本文件僅為計畫，尚未撰寫任何實作程式碼）
- **場景定位**：Hero-Lab 滾動時間軸壓軸，銜接 Scene 03 末端 → 「選購守宮 (Shop Geckos)」按鈕
- **主要檔案**：`components/DnaGeckoParticles.vue`（3D 核心）、`components/HeroDnaGecko.vue`（DOM 疊層 / Bloom）、`pages/hero-lab.vue`（滾動容器）

---

## 0. 現況架構錨點（實作前提，附精確行號）

| #   | 現況事實                                                                                                   | 精確位置                                                                   | 對本工程的意義                                                              |
| --- | ---------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | --------------------------------------------------------------------------- |
| A   | 原生 scroll 驅動（非 GSAP ScrollTrigger）；scroll→`scrubTo(progress,false)`                                | `HeroDnaGecko.vue` `getNativeHeroScrollProgress` L85–102、`scrubTo` L3524+ | 終章進度**接回既有管線**，禁另開 timer                                      |
| B   | 進度分段 `journeySegments` = `scene01 / transition / scene03 / cards / next / placeholder(end:1)`          | `DnaGeckoParticles.vue` L1619–1626                                         | 終章 = **`placeholder` 段**；`next` 段是進場暗幕                            |
| C   | 命令式 3D 物件於 setup 建立，`useLoop().onBeforeRender` 逐幀驅動                                           | 組裝 L2868–2874、迴圈 L3193                                                | 蛋/心跳/裂解進**同一** `onBeforeRender`，共用 `elapsed`/`dt`                |
| D   | PMREM RoomEnvironment 已建立且共享給 Physical 材質；`envRT`                                                | `initEnvMap()` L3172–3191                                                  | 蛋殼玻璃複用 `envRT.texture`，零額外環境成本                                |
| E   | 全域 UnrealBloom（strength 0.18 / threshold 0.95）                                                         | `HeroDnaGecko.vue` L347–349                                                | 心跳輝光**只能靠蛋體局部 emissive**，不可調全域 bloom                       |
| F   | 現行終章 = DOM overlay（暗幕 tile-wipe + 圓柱/水面/面板，z-index 6600）                                    | `HeroDnaGecko.vue` L365–385（結構）/ L525–697（樣式）                      | 暗幕 tile-wipe **保留當進場**；圓柱 DOM **汰換**；DOM 按鈕沿用 z 慣例       |
| G   | 場景 `<primitive :object="groupRef" />`；root `group`（L484）組裝子群組；`disposables[]` 統一清理（L3789） | L2868 / L3789                                                              | 新增 `eggGroup` 掛 root `group`，資源 push 進 `disposables`                 |
| H   | Debug 跳關 `window.__hero.scene3(c, n, p)`：直接設卡片/next/placeholder 進度                               | L3663                                                                      | 各 Phase 主要驗證槓桿                                                       |
| I   | `emit('next-scene-progress', currentNextSceneProgress)` 已存在；**placeholder 未 emit**                    | L3308–3311                                                                 | HeroDnaGecko 需要 placeholder 值時，於原生模式自行由 scroll 推導（見 §2.0） |
| J   | render 節流：`bottomRenderMode` `'always'`/`'manual'`，`wakeBottomRender()` 喚醒                           | L1658–1674、L3428                                                          | 終章動畫期間須維持 `'always'`                                               |

---

## 1. 已核准決策（2026-07-30 使用者拍板）

1. **獨立 `uFinaleReveal`（0→1）**，與 Scene 03 `uIntroReveal` 解耦。
2. **DOM 圓柱 placeholder 直接汰換**（非並存）。
3. **心跳 `sin(uTime)` 時間驅動**＝「轉動一律滾輪驅動」約束的書面豁免（唯一例外）。
4. 按鈕導向 **`/shop`**。

---

## 2. 終章進度模型（本計畫的骨幹）

### 2.0 進度來源與 `uFinaleReveal` 定義

- **`next` 段**（`currentNextSceneProgress` 0→1）：Scene 03 退場 + 暗幕 tile-wipe 掃入。**不放蛋**。
- **`placeholder` 段**（`currentPlaceholderProgress` 0→1）：**整個終章劇本**。故定義：

  ```
  uFinaleReveal := currentPlaceholderProgress        // 0→1，等同 placeholder 段
  ```

- **DOM 端（原生 scroll 為 active 路徑）** 需要 placeholder 進度來淡出暗幕。不新增 emit，改在 `HeroDnaGecko.vue` 由 scroll 推導（與既有 `nativeNextSceneProgress` 同法）：

  ```
  placeholderSegEnd = 1                               // journeySegments 'placeholder'.end
  nextSegEnd        = segments.find('next').end
  nativePlaceholderProgress = clamp(
      (nativeHeroProgress - nextSegEnd) / (1 - nextSegEnd), 0, 1)
  ```

### 2.1 劇本節拍表（`p = uFinaleReveal = placeholderProgress`）

| 區間 p    | 節拍                                        | 主驅動                            | 對應 Phase    |
| --------- | ------------------------------------------- | --------------------------------- | ------------- |
| 0.00–0.12 | 暗幕淡出 + 蛋淡入/scale-in（0.74→1.0）      | scroll                            | P1 + P5(暗幕) |
| 0.12–0.60 | 蛋懸浮，胚胎心跳律動                        | scroll(presence) + **time(心跳)** | P1/P2         |
| 0.60–0.70 | 裂紋前輝光累積                              | scroll                            | P3            |
| 0.70–0.90 | Voronoi 裂紋由蛋底向上蔓延 + `#ff7a00` 亮化 | scroll                            | P3            |
| 0.85–0.92 | 蛋底 3D-noise `discard` 消融，液體滲出      | scroll                            | P3            |
| 0.88–0.98 | metaballs 下墜 + `smin` 融合                | scroll                            | P4            |
| 0.95–1.00 | morph→圓角矩形 + DOM 按鈕 crossfade 接管    | scroll                            | P4/P5         |

> 節拍點與規格書「裂解 0.7→0.95、metaballs→按鈕趨近 1.0」一致。

### 2.2 節奏調校備註（非阻塞）

- `placeholderSceneWheelLen = nextSceneWheelLen * 0.75`（L1614）≈ 2500 wheel 單位。若「懸浮 + 心跳」段（0.12–0.60）感覺太短，將 0.75 調大即可延長終章滾動距離，**不影響前段**。列為 P2 完成後的體感微調項。

### 2.3 Scene 03 清場協調（P1 必辦，易漏）

- 現況 `scene3RevealActive = seamB>0.012 || finalDna>eps`（L3223）在終章仍為真（`sweep=1 → seamB=2`），故 **backbone / gravityWellGrid / cards 在蛋出現時仍會被畫**，會與蛋重疊。
- P1 需加 `scene3ExitFade`，令這些元素在 `next`→`placeholder` 早段淡出清空：
  ```
  scene3ExitFade = 1 - smoothstep(currentNextSceneProgress, 0.35, 0.9)   // next 段後半清場
  ```
  套用到 `backboneSampleMat.uAlpha`、`scene3GravityWellGridMat.uFade`、`heroCardsGroup` 相關 opacity，並在 `scene3ExitFade≈0` 時關 `visible`。蛋出現前舞台須乾淨。

---

## 3. 分階段實作路線圖（含檔案 / 變數 / shader / 錨點）

### Phase 1 — 終章骨架 + 科技玻璃蛋（靜態）

**改動檔案**：`DnaGeckoParticles.vue`

**新增常數 / 物件**（插入點：`buildHeroCards()` 之後、`group.add(...)` 之前，約 L2863）

```
EGG_Y_STRETCH = 1.35            // 卵形 Y 拉伸
EGG_BASE_RADIUS = 1.05
eggGroup: THREE.Group           // position (0, 0.1, 2.0)，初始 visible=false
eggShellGeo: SphereGeometry(EGG_BASE_RADIUS, 96, 96)
eggShellMat: MeshPhysicalMaterial {
  color '#eaf6ff', metalness 0, roughness 0.05, ior 1.52, thickness 0.9,
  transmission 0.95, transparent, iridescence 0.8, iridescenceIOR 1.3,
  clearcoat 0.6, clearcoatRoughness 0.08, envMapIntensity 1.1, depthWrite false }
→ eggGroup.add(mesh); group.add(eggGroup); disposables.push(geo, mat)
```

**envMap 綁定**（`initEnvMap()`，緊接 `pyramidGlassMat.needsUpdate = true` 之後）

```
eggShellMat.envMap = envRT.texture; eggShellMat.needsUpdate = true
```

**`onBeforeRender` 驅動**（placeholder 平滑收斂之後，約 L3322）

```
finaleReveal = clamp(currentPlaceholderProgress, 0, 1)
eggPresence  = smoothstep(finaleReveal, 0.0, 0.12)
eggGroup.visible = eggPresence > 0.001
if visible:
  s = lerp(0.74, 1.0, eggPresence)
  eggGroup.scale.set(s, s*EGG_Y_STRETCH, s)
  eggShellMat.opacity = eggPresence
+ Scene 03 清場（§2.3）
```

**驗證 KPI**

- `window.__hero.scene3(0, 1, 0.5)` → scene-graph 出現 `eggGroup`（`children[0]` 為蛋 mesh）。
- 蛋面反射 RoomEnvironment 高光；Y 徑/ X 徑比 ≈1.35。
- `scene3(0,1,0)` → 蛋不可見（回捲可逆）；舞台無 backbone/cards 殘影。
- console 無 error；`window.__hero.info()` triangles/calls 無異常暴增，fps 穩。

**不做**：心跳、裂解、液體、按鈕、DOM 汰換。**回滾**：移除三段插入即可，無外部相依。

---

### Phase 2 — 胚胎全像 + 物理心跳

**改動檔案**：`DnaGeckoParticles.vue`

**新增物件**（eggGroup 內）

```
embryoMesh: 簡化守宮輪廓（先用低面 IcosahedronGeometry placeholder；日後換簡模）
embryoMat : MeshBasicMaterial/ShaderMaterial，半透明 additive，color '#ff8a3d'，
            uniform uHeartbeat 控 emissive 強度
heartLight: PointLight('#ff8a3d', intensity, distance≈4)，置蛋心
→ eggGroup.add(embryoMesh, heartLight)
```

**心跳公式**（`onBeforeRender`，時間驅動豁免）

```
HEART_BPM = 60 → ω = 2π * (60/60) = 2π
pulse = sin(elapsed * ω)                        // −1..1
uHeartbeat        = 0.55 + 0.35 * pulse         // emissive 基準+振幅
heartLight.intensity = base + amp * pulse
// 依 finaleReveal 讓心跳在 0.12–0.60 完全顯現、之後隨裂解漸弱
heartGate = smoothstep(finaleReveal,0.08,0.2) * (1 - smoothstep(finaleReveal,0.7,0.95))
uHeartbeat *= heartGate; heartLight.intensity *= heartGate
```

**Bloom 隔離要點**：心跳只調 **蛋/胚胎 emissive 與 local light**，UnrealBloom `strength` 不動。蛋殼 emissive 峰值需略高於 threshold 0.95 才會泛光，靠 emissive 而非全域。

**驗證 KPI**

- 胚胎輝光 ≈1 秒/次脈動（可用 console log `uHeartbeat` 觀察）。
- **其餘場景 bloom 不隨心跳律動**（截其他發光物件比對）。
- 靜止（不滾動）時仍脈動 → 證明時間驅動豁免生效。
- 裂解區間（p>0.7）心跳漸熄。

**回滾**：移除 embryo/heartLight 與心跳段。

---

### Phase 3 — 蛋殼 Voronoi 裂紋 + 3D Noise 消融

**改動檔案**：`DnaGeckoParticles.vue`（蛋殼材質改注入式）

**材質策略**：維持 `MeshPhysicalMaterial`（保 PBR/透射/虹彩），以 `onBeforeCompile` 注入片段程式，**不改整段自寫 shader**，避免遺失環境高光。

```
eggShellMat.onBeforeCompile = (shader) => {
  shader.uniforms.uFinaleReveal = eggUniforms.uFinaleReveal   // 共享 ref
  shader.uniforms.uTime         = eggUniforms.uTime
  // 注入 varying vLocalPos（物件座標，供 Y 加權裂紋與 discard）
  // 於 fragment 末段疊加裂紋 emissive、於前段做 discard
}
每幀：eggUniforms.uFinaleReveal.value = finaleReveal
```

**GLSL 設計（片段，設計稿非最終碼）**

```
// 由蛋底(-Y)向上：yNorm ∈ [0(底),1(頂)]
float yNorm  = vLocalPos.y / EGG_BASE_RADIUS * 0.5 + 0.5;
float front  = (uFinaleReveal - 0.7) / 0.20;             // 0.7→0.9 蔓延
float wave   = smoothstep(1.0 - front, 1.0 - front + 0.15, 1.0 - yNorm);

// Voronoi F1（cellular）
float F1     = voronoiF1(vLocalPos * FREQ + uTime*0.05);
float crack  = (1.0 - smoothstep(0.0, CRACK_W, F1)) * wave;
vec3  glow   = vec3(1.0, 0.478, 0.0) * crack * EDGE_GLOW;  // #ff7a00
gl_FragColor.rgb += glow;                                  // 亮化，供 bloom

// 3D noise 消融（0.85→0.95）
float diss   = (uFinaleReveal - 0.85) / 0.10;
if (simplex3d(vLocalPos * DISS_FREQ) + diss * DISS_SCALE < (1.0 - yNorm)) discard;
```

- `voronoiF1` / `simplex3d`：內嵌經典 GLSL 實作（無外部相依）。
- 需 `derivatives` extension（既有 shader 已用，L1101）。

**驗證 KPI**

- p 0.70→0.90：裂紋自底向上、`#ff7a00` 亮化，bloom 有反應。
- p≥0.85：蛋底 discard 破口，露出胚胎/內部。
- 反向滾動裂紋回收（可逆）。
- fps 不掉（Voronoi/Simplex 每片段成本可控；蛋面片數有限）。

**回滾**：移除 `onBeforeCompile` 與每幀 uniform 寫入。

---

### Phase 4 — 2D SDF Metaballs 液態金屬（→圓角矩形）

**改動檔案**：`DnaGeckoParticles.vue`（新增 ortho 全螢幕層）或 `HeroDnaGecko.vue`（獨立疊層 canvas）。**建議**：TresCanvas 內新增 **Orthographic 全螢幕 quad**（省一個 GL context）。

**新增物件**

```
fluidUniforms = { uFinaleReveal, uTime, uResolution, uButtonRect(vec4: cx,cy,halfW,halfH), uRadius }
fluidMat = ShaderMaterial（transparent, depthTest false, depthWrite false, blending Normal）
fluidQuad = Mesh(PlaneGeometry(2,2), fluidMat)，掛在對齊相機的 ortho 或 screen-space 層
可見條件：finaleReveal > 0.85
```

**降採樣策略（效能）**：以較低解析 render target 繪 fluid 再放大，或直接在片段內限制迭代數。目標 ≥55fps。現行 `dpr` 已夾 `[1,1.5]`，fluid 層再降一級。

**GLSL 設計（片段，螢幕像素空間）**

```
vec2 P = gl_FragCoord.xy;                         // 像素座標
// 落滴：N 顆自蛋底流出、重力下墜（相位由 uFinaleReveal 推進）
float d = 1e9;
for (i in 0..N) {
  vec2  c = dropletCenter(i, uFinaleReveal, uTime);
  float r = dropletRadius(i, uFinaleReveal);
  d = smin(d, length(P - c) - r, K_FUSE);         // smin 水銀融合
}
// 圓角矩形 SDF（目標按鈕）
vec2  q = abs(P - uButtonRect.xy) - uButtonRect.zw + uRadius;
float boxSDF = min(max(q.x,q.y),0.0) + length(max(q,0.0)) - uRadius;
// morph 0.95→1.0
float m = clamp((uFinaleReveal - 0.90) / 0.10, 0.0, 1.0);
float finalSDF = mix(d, boxSDF, m);
float alpha = smoothstep(1.5, -1.5, finalSDF);    // 邊緣 AA
// 液態金屬著色：法線 = normalize(dFdx,dFdy of SDF)，envMap-ish 高光 + amber 漸層
```

- `smin`（多項式版，避免 log/exp 成本）：`h=clamp(0.5+0.5*(b-a)/k,0,1); mix(b,a,h)-k*h*(1-h)`。
- `uButtonRect` / `uRadius` 由 §5 座標映射每幀寫入（與 DOM 按鈕同一真值來源）。

**驗證 KPI**

- 液滴下墜 + `smin` 融合呈水銀張力。
- p→1 融合體平滑 morph 成置中圓角矩形，位置/圓角 = `uButtonRect`。
- 降採樣下 ≥55fps（`window.__hero.info()` 觀察）。

**回滾**：移除 fluidQuad / fluidMat / 每幀 uniform。

---

### Phase 5 — DOM「選購守宮」按鈕無縫接管 + 暗幕汰換

**改動檔案**：`HeroDnaGecko.vue`（DOM + 樣式 + placeholder 推導）、`DnaGeckoParticles.vue`（暴露 button rect）

**5a 暗幕汰換 / 舞台淨空**

- 移除 `.next-scene-stage` 圓柱/水面/面板 DOM（L371–384）與對應樣式（決策 2）。
- `.next-scene-blackout` 保留 tile-wipe 進場（`next` 段），並於 **placeholder 段以 `nativePlaceholderProgress` 淡出背景**（opacity 1→~0.12 保留微暈），讓 canvas 蛋透出：
  ```
  --finale-curtain-opacity = 1 - smoothstep(nativePlaceholderProgress, 0.0, 0.12)
  ```

**5b DOM 按鈕**

```
<button class="shop-btn"> 選購守宮 / Shop Geckos </button>   // 疊層最上，初始 opacity:0; pointer-events:none
click → navigateTo('/shop')  // 決策 4
```

**5c 座標映射（單一真值來源）**

- 按鈕的目標矩形以 **CSS px 常數** 定義：`btnCx, btnCy`（螢幕中央偏下）、`btnHalfW, btnHalfH`、`btnRadius`。
- 同一組值換算成 drawingBuffer 像素寫入 `fluidUniforms.uButtonRect`：
  ```
  dprScale = drawingBufferWidth / cssWidth      // 對映 CSS px → GL px
  uButtonRect = vec4(btnCx*dprScale, (cssH-btnCy)*dprScale, btnHalfW*dprScale, btnHalfH*dprScale)
  uRadius     = btnRadius * dprScale
  // 注意 gl_FragCoord.y 由底向上 → cssY 需翻轉
  ```
- DOM 按鈕 CSS 用同組 `btn*` 值定位，確保**逐像素重合**。

**5d 交接（防閃爍 crossfade）**

```
handoff = smoothstep(finaleReveal, 0.97, 1.0)
fluidMat.opacity(圖形) = 1 - handoff             // WebGL 淡出
shop-btn: opacity = handoff; pointer-events = handoff>0.5 ? 'auto':'none'
GSAP spring: scale 1.08→1.0（handoff 起 220ms）
```

重疊淡出/淡入 ~120ms，不硬切（對齊 L3452「canvas 全程不硬切」）。

**驗證 KPI**

- SDF 矩形與 DOM 按鈕**截圖疊比逐像素重合**。
- 交接無單幀空白/亮跳（逐幀截圖檢查）。
- hover 增亮、click → `/shop`。
- `resize_window` 後仍對齊（座標映射依 resolution 重算）。

**回滾**：DOM 按鈕與暗幕改動獨立，可單獨還原；圓柱 DOM 汰換不可逆（如需保留，先備份 L365–697 樣式）。

---

## 4. 效能與無縫銜接預防（彙整）

| 風險                       | 預防                                                         |
| -------------------------- | ------------------------------------------------------------ |
| 全螢幕 metaballs 片段成本  | 降採樣 RT / 限制迴圈 N；`smin` 用多項式版                    |
| 透射蛋 + 全域 bloom 雙負擔 | 蛋只在 `finaleReveal>0` 進 render；非終章維持 `'manual'`     |
| DOM↔SDF 圓角不吻合         | §5c 單一 px 真值來源；`gl_FragCoord` 螢幕空間                |
| 交接閃爍                   | §5d crossfade，不硬切                                        |
| Scene 03 殘影              | §2.3 `scene3ExitFade` 清場                                   |
| 心跳污染全域 bloom         | 只調局部 emissive/light（§Phase 2）                          |
| HMR 對 shader 不可靠       | 改 shader/新元件後**重啟 dev server**；3D 驗證走 scene-graph |
| 回捲不可逆                 | 所有節拍以 `finaleReveal` 純函數映射，天然可逆               |

---

## 5. 逐階段交付與簽核

| Phase | 交付                | 主要驗證指令                                      |
| ----- | ------------------- | ------------------------------------------------- |
| P1    | 靜態玻璃蛋 + 清場   | `__hero.scene3(0,1,0.5)` + scene-graph + `info()` |
| P2    | 胚胎 + 心跳         | 觀察 `uHeartbeat`；比對他物件 bloom 不動          |
| P3    | 裂紋 + 消融         | `scene3(0,1,p)` 掃 p=0.6→0.95                     |
| P4    | metaballs + morph   | `scene3(0,1,p)` p=0.85→1.0；`info()` fps          |
| P5    | DOM 按鈕 + 暗幕汰換 | 截圖疊比、resize、click→/shop                     |

**動工協定**：本文件為計畫，動工前不寫實作碼。核准後請指示「開始 Phase N」；預設逐階段實作 → 瀏覽器/scene-graph 驗證 → 回報 → 再進下一階段。
