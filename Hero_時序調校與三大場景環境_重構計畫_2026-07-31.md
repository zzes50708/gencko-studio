# Hero-Lab：完整時序調校 + 三大場景環境 — 重構與優化計畫

- **版本**：v1（2026-07-31）
- **狀態**：待批准（本文件僅為計畫，未動任何實作程式碼）
- **依據**：使用者提供螢幕錄影 `螢幕錄製 2026-07-31 142528.mp4`（19.77s / 30fps / 1908×884），已逐格抽樣核對
- **主要檔案**：`components/DnaGeckoParticles.vue`（4443 行，3D 核心）、`components/HeroDnaGecko.vue`（DOM/Bloom）、`pages/hero-lab.vue`

> 註：本計畫承接既有終章實作（蛋/裂解/流體皆已存在於現況檔案），屬**重構/調校**，非從零新建。

---

## 0. 影片核對結論（逐格證據 + 根因）

| 時間 | 觀察到的畫面                                     | 判定                         | 根因（對照程式碼）                                                                                                                             |
| ---- | ------------------------------------------------ | ---------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| 02s  | DNA 螺旋 + Logo 盤，守宮未現                     | —                            | 守宮 reveal 尚在進行                                                                                                                           |
| 03s  | 守宮完全長成、發光在 DNA 上                      | **Bug 1** 守宮展示 <1s       | `scene01GridHoldSpan=420`（L1935）停留過短，且守宮 reveal 佔滿整段 span，完成即被斜帶切走                                                      |
| 06s  | 「SCIENTIFIC BREEDING…」被黑色斜帶壓過一閃       | **Bug 2** 文字閃現           | 斜帶 `sweep` 線性掃過（L3189 附近），文字可讀窗口只有數幀，無停留                                                                              |
| 11s  | Scene03 脊椎 + 重力井 + 卡片                     | —                            | 正常                                                                                                                                           |
| 13s  | 卡片消失、脊椎懸空、網格淡                       | 過渡開始                     | 脊椎在 `next` 段沿 +Y 滑走（`BACKBONE_NEXT_SCENE_RISE`）                                                                                       |
| 14s  | **全黑**（只剩 DOM 暗幕青/橘徑向暈）             | **Bug 3** 死黑斷檔 ~1.5s     | 蛋入場綁 `placeholder` 段（`eggEntrance=smoothstep(finaleReveal,0,0.3)` L3867），但脊椎已於 `next` 段離場 → `next` 段無 3D 內容，只剩 DOM 暗幕 |
| 15s  | 玉綠色玻璃蛋懸浮（光學漂亮）                     | —                            | 蛋 OK                                                                                                                                          |
| 17s  | 蛋**下半直接消失**、半殼懸空、底下一小坨脫節液體 | **Bug 4a/4b** 硬切、液體脫節 | 蛋底 `discard`（bottomBias L1148）硬切；`eggShardMesh.visible=false`（L3897）無碎片；流體 `finaleFluidMat` source 未對齊蛋螢幕位置             |
| 18s  | 半殼仍在，「選購守宮」按鈕在**下方獨立淡入**     | **Bug 4c** 蛋沒硬化成按鈕    | morph 階段蛋頂殼未淡出，DOM 按鈕只是 crossfade 疊上                                                                                            |
| 全程 | 三場景皆黑底，無霧/孢子/數據流/水霧/聚光         | **Part 二** 環境未實作       | 無 `FogExp2`/`scene.background`/`SpotLight`/水面                                                                                               |

---

## 1. 架構前提（動工前必讀）

- **本專案 Hero 不使用 GSAP ScrollTrigger**，而是**原生 scroll → `scrubTo(progress)`**（`HeroDnaGecko.vue` L85–102、`scrubTo` L3524+）把 0→1 頁面捲動映射到 `totalJourneyWheelLen`，再切分到各 stage span。
  - → 你要求的「斜帶展開時滾動阻尼變重、強迫停留」，**最輕量做法不是導入 GSAP**，而是**在 scroll→timeline 的映射中插入「停留配額 (dwell allocation)」**：讓某段 timeline 佔用更多捲動距離（等效於阻尼變重）。導入 GSAP 需重寫整條 native-scroll 管線，風險高、無必要。
- **三大場景共用同一個 Three scene**，故環境（霧/背景色）採**單一 `FogExp2` + 單一背景**，其 `color`/`density` 每幀依 `journeyProgress` 在三套預設間插值，而非建三個場景。這是最省的做法。

---

## 2. 分階段路線圖

### ── Part 一：時序與過渡重構（Bug 1–4）──

#### Phase 0 — 時間軸「配額表」地基（Bug 1/2 前置）

**問題**：現況各 stage span 是散落常數（`introRevealSpan`/`firstExitSpan`/…），要插入停留區很難維護。
**做法（`DnaGeckoParticles.vue`）**：

- 抽出一張**單一真值的「捲動配額表」**（各 stage 佔多少 wheel 長度 + 是否為 dwell plateau），`timelineBreaks` / `journeySegments` / `scrubTo` 全部改讀此表。
- 新增 `dwell` 概念：某區間 timeline 值**維持不變**（畫面凍結可讀/可賞），只消耗捲動距離。
  **KPI**：重構後畫面與現況**逐格一致**（純結構重整、零視覺變化），`__hero.state()` 各值不變。**這是安全網 Phase，先證明無回歸再調數值。**

#### Phase 1 — 守宮 Hero Time 延長（Bug 1）

- 在守宮 reveal **完成後**、斜帶 `firstExit` **開始前**，插入 `geckoHoldSpan` dwell（守宮定格於畫面中央、僅隨滾輪/慣性緩轉）。
- 守宮完整可見時間目標：**≥ 2.5 秒**等效捲動（現況 <1s）。
- 轉動遵守「滾輪驅動」約束（`[[wheel-driven-rotation]]`）：dwell 期間守宮轉動仍由滾輪帶動，不加時間自轉。
  **KPI**：`__hero.jump(gecko完成點)` 後，需滾動一段距離守宮才開始被斜帶切；守宮居中旋轉展示 ≥2.5s。

#### Phase 2 — 斜帶文字停留（Bug 2）

- 在斜帶 `sweep` 掃到「文字完全露出且未被黑帶壓住」的相位（約 sweep≈0.45–0.6）插入 dwell plateau，佔用額外捲動距離，`--hero-initial-copy-opacity` 於此區間拉滿並維持。
- 文字可讀停留目標：**≥ 2 秒**等效捲動。
- 保留斜帶轉場本身（決策：保留斜帶）。
  **KPI**：慢滾到斜帶中段，文字清晰靜止可讀 ≥2s，續滾才進 Scene 03。

#### Phase 3 — Scene03 → 蛋 交叉滑動，消除死黑（Bug 3）

- **核心**：把蛋入場從「等 `placeholder` 段」提前到**與脊椎離場重疊的 `next` 段**：
  - 脊椎 Group 隨 `next` 進度沿 +Y 平滑滑出上緣（沿用 `BACKBONE_NEXT_SCENE_RISE`，微調曲線）。
  - **同時** 蛋 Group 從螢幕下方（y≈−5.2）滑入中央，入場進度改綁 **`next`+`placeholder` 銜接**而非只 placeholder。
- **汰除死黑來源**：`next` 段的 DOM 暗幕（`next-scene-blackout`，`HeroDnaGecko.vue` L365+）改為**不覆蓋 canvas**——保留其作為背景暗化/氛圍，但背景在蛋登場時淡成微暈，讓 canvas 蛋全程可見（沿用細節計畫 v2 §2.0 的 `nativePlaceholderProgress` 推導，向前延伸涵蓋 `next` 尾段）。
- 目標：**0 幀純黑**，脊椎上滑與蛋下滑在畫面上**時間重疊**。
  **KPI**：`__hero` 掃 `next`→`placeholder` 全程逐格截圖，**無任一幀全黑**；脊椎與蛋同時在場至少數幀。

#### Phase 4 — 蛋殼碎裂消融 + 蛋液連續流下 + 硬化成按鈕（Bug 4，狀態機鎖定）

以 `uFinaleReveal`（=`currentPlaceholderProgress`）為**單一狀態機時鐘**，嚴格分區、可逆：

| p 區間    | 狀態         | 修正重點                                                                                                                                 |
| --------- | ------------ | ---------------------------------------------------------------------------------------------------------------------------------------- |
| 0.70–0.82 | 蛋殼碎裂     | 啟用 `eggShardMesh`（現況被 `visible=false` 關掉），蛋殼沿裂紋**裂成碎片外擴/下墜**，取代「蛋底直接 discard」的硬切                      |
| 0.80–0.90 | 消融         | 碎片與殼底以 noise 漸融（alpha+scale 收縮），非瞬間消失                                                                                  |
| 0.82–0.95 | 蛋液水銀流下 | `finaleFluidMat` 的 `source` 對齊**蛋底當前螢幕座標**（投影 eggGroup 底部到 gl_FragCoord 空間），液體從蛋底**連續**滲出下墜，`smin` 融合 |
| 0.90–1.00 | 硬化聚合     | 流體 `mix` → roundedBox；**同時蛋頂殼 alpha 淡出**（現況缺這步）→ 畫面只剩硬化矩形                                                       |
| 0.97–1.00 | DOM 接管     | WebGL 圖形淡出、DOM 按鈕 crossfade + GSAP spring（現況已有，銜接點對齊硬化完成幀）                                                       |

**KPI**：`p` 0.7→1.0 逐格：看得到碎片、看得到液體**從蛋底連續**流下、蛋殼在按鈕成形時**消失**（不與按鈕並存）；反向可逆。

---

### ── Part 二：三大場景環境客製（輕量 WebGL）──

> 共用單一 `FogExp2` + 單一背景平面，`color`/`density` 每幀依 `journeyProgress` 在三套預設插值。所有粒子用 **InstancedMesh / Points + additive**，數量上限受控，gated 到各自場景（非該場景時 `visible=false`，零成本）。

#### Phase 5 — 場景一「生物基因艙」（暖橘）

- `FogExp2` 暖橘（如 `#3a1a0e`）低密度大氣霧。
- **發光基因孢子**：`Points`（~250–400 顆）橘色 additive，緩慢上下漂浮（`sin(uTime*相位)`，滾輪不影響漂浮=氛圍例外，需標注豁免或改為極慢）。
- **假景深 DoF**：不做 post-DoF（太重）。改以**依 z 深度調 point size + 柔邊 alpha**模擬散景，近清晰遠糊。
  **KPI**：Scene01 有暖橘霧感 + 漂浮孢子，遠近糊；fps 不掉（`__hero.info()`）；離開 Scene01 孢子 `visible=false`。

#### Phase 6 — 場景二「數據脊椎矩陣」（冷灰）

- 霧/背景插值到**冷深灰**（如 `#14161a`）。
- **全像數據流**：既有重力井網格 shader 疊加**細微 scan line / 代碼流**（沿網格 uv 方向流動的細亮線，shader 內完成，零額外幾何）。
- **重力井脈衝**：把**卡片公轉相位**（`currentCardOrbit`/各卡角度）當作 pulse 中心傳入網格 shader，網格在卡片經過處**亮起**（`pulse = exp(-dist²)` 隨卡片移動）。
  **KPI**：Scene03 冷灰氛圍 + 數據流動 + 卡片經過時網格局部亮起；純 shader、無新 drawcall 暴增。

#### Phase 7 — 場景三「核心孵化溫室」（終章）

- 極薄保濕水霧（霧密度略升 + 少量柔粒子）。
- **頂部 SpotLight** 直射蛋。
- **體積光束**：不做 raymarch 體積光（太重）。以**加法漸層光錐 Mesh**（ConeGeometry + additive shader，越靠光源越亮、邊緣柔）模擬水霧中的丁達爾光束。
- **水面**：蛋下方一片 plane，shader 做**微波紋 + 反射蛋體心跳橘光**（取心跳 `uHeartbeat` 當反射亮度，ripple 用 noise，非真實 SSR）。
  **KPI**：蛋被頂光照亮、霧中見光束、水面隨心跳泛橘波動；fps ≥55。

---

## 3. 效能與風險預防

| 風險                     | 預防                                                                       |
| ------------------------ | -------------------------------------------------------------------------- |
| 導入 GSAP 重寫管線       | **不導入**；以 scroll→timeline 配額表 + dwell plateau 達成「阻尼變重」     |
| Phase 0 重構造成視覺回歸 | Phase 0 定位為「零視覺變化」安全網，逐格比對現況                           |
| 多場景粒子/霧成本        | 單一 Fog/背景插值；粒子 InstancedMesh/Points + gated visible；非場景時不畫 |
| 真 DoF / 體積光太重      | 用 point-size 假散景、加法光錐假體積光，皆 shader 內完成                   |
| 蛋液對齊蛋底             | 每幀投影 eggGroup 底部世界座標 → gl_FragCoord，餵 `finaleFluidMat.source`  |
| dwell 破壞進度條線性     | 進度條讀同一份配額表，dwell 也計入 wheel 長度，手感一致                    |
| 漂浮孢子/水波時間驅動    | 屬氛圍非「轉動」，比照心跳列**書面豁免**；或改極慢滾輪關聯                 |
| HMR 對 shader 不可靠     | 改 shader/新元件後重啟 dev server；3D 驗證走 scene-graph + 逐格截圖        |

---

## 4. 交付順序與簽核

建議順序：**Phase 0（安全網）→ 1 → 2 → 3 → 4**（時序先修好，體驗才順）**→ 5 → 6 → 7**（再疊場景氛圍）。

每個 Phase：實作 → dev server + 逐格截圖/scene-graph 驗證 KPI → 回報 → 批准後進下一階段。

**動工協定**：本文件為計畫，動工前不寫實作碼。請審查；核准後指示「開始 Phase 0」（或指定起始 Phase / 調整順序）。

---

## 附：可調參數速查（供你決策）

- 守宮 Hero Time 目標：預設 **≥2.5s** 等效捲動（可改）
- 斜帶文字停留：預設 **≥2s**（可改）
- 孢子數量：預設 **250–400**（效能/密度取捨）
- 場景配色：S1 暖橘 `#3a1a0e` / S2 冷灰 `#14161a` / S3 溫室霧（待你定色）
- 按鈕導向：`/shop`（沿用前案決策）
