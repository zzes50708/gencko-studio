git checkout main
git status
git fetch origin
git push origin main

直接更新git checkout main && git fetch origin && git push --force-with-lease origin main

git add .
git commit -m "redeploy"
git push origin main
git push -f